#include "main.h"
/**
 * _isdigit - It checks if a given character is a digit
 * @c: A character to be checked
 *
 * Return: In case of digit => 1, Otherwise => 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
